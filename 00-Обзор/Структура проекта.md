# Структура проекта

## Двойная архитектура

Проект содержит **две параллельные реализации** контроллера:
- **Python** (`quadropted_controller/`) -- оригинальная реализация, быстрая итерация разработки
- **C++** (`quadropted_controller_cpp/`) -- высокопроизводительная версия, продакшен

Обе реализации имеют **идентичную функциональность** и подтверждённую численную точность через кросс-валидацию.

```
WalkingRobotSim/
├── .github/workflows/
│   ├── ci.yml                    # CI: Docker build + базовый тест
│   └── test.yml                  # Полное тестирование симуляции
│
├── src/
│   ├── docker/                   # Docker инфраструктура
│   │   ├── Dockerfile            # 10-этапная сборка
│   │   ├── compose.yml           # Docker Compose (GUI passthrough)
│   │   └── cyclonedds.xml        # DDS конфигурация
│   │
│   ├── gazebo_sim/               # Пакет симуляции (оркестрация)
│   │   ├── launch/
│   │   │   ├── launch_python.launch.py        # Топ-левел launch (Python)
│   │   │   ├── launch_cpp.launch.py           # Топ-левел launch (C++)
│   │   │   └── gazebo_multi_nav2_world.launch.py  # Мультиробот + Nav2
│   │   ├── config/
│   │   │   ├── nav2_params.yaml    # Параметры Nav2
│   │   │   ├── ekf.yaml            # Extended Kalman Filter
│   │   │   ├── robot_control.yaml  # ros2_control контроллер
│   │   │   ├── gz_bridge.yaml      # ROS-Gazebo bridge
│   │   │   └── robots.yaml         # Определение роботов
│   │   ├── world/
│   │   │   └── cafe.world          # Gazebo мир
│   │   ├── maps/                   # Карты навигации (YAML + PGM)
│   │   ├── rviz/                   # RViz конфигурации
│   │   ├── models/                 # Gazebo модели
│   │   └── src/test_action.py      # Тестовый action узел
│   │
│   ├── quadropted_controller/      # Основной пакет управления (Python)
│   │   ├── scripts/
│   │   │   ├── robot_controller_gazebo.py     # Главный узел (точка входа)
│   │   │   ├── QuadrupedOdometryNode.py       # Узел одометрии
│   │   │   ├── cmd_vel_pub.py                 # Twist → RobotVelocity адаптер
│   │   │   ├── RobotController/
│   │   │   │   ├── RobotController.py         # Главный контроллер (state machine)
│   │   │   │   ├── GaitController.py          # Базовый класс походки
│   │   │   │   ├── TrotGaitController.py      # Рысь (диагональные пары)
│   │   │   │   ├── CrawlGaitController.py     # Ползание (последовательные ноги)
│   │   │   │   ├── RestController.py          # Покой (неподвижность)
│   │   │   │   ├── StandController.py         # Стойка (вращение на месте)
│   │   │   │   ├── PIDController.py           # IMU roll/pitch компенсация
│   │   │   │   └── StateCommand.py            # Data classes State/Command
│   │   │   ├── ForwardKinematics/
│   │   │   │   └── robot_FK.py                # Прямая кинематика
│   │   │   ├── InverseKinematics/
│   │   │   │   └── robot_IK.py                # Обратная кинематика
│   │   │   └── RoboticsUtilities/
│   │   │       └── Transformations.py         # Матрицы поворота/трансформации
│   │   └── test/                   # 13 pytest файлов
│   │
│   ├── quadropted_controller_cpp/  # Основной пакет управления (C++)
│   │   ├── include/quadropted_controller_cpp/
│   │   │   ├── utils/
│   │   │   │   ├── rotation_matrices.hpp      # Матрицы вращения
│   │   │   │   ├── homogeneous_transforms.hpp # Однородные преобразования
│   │   │   │   └── message_builders.hpp       # Структуры сообщений
│   │   │   ├── kinematics/
│   │   │   │   ├── forward_kinematics.hpp     # Прямая кинематика
│   │   │   │   └── inverse_kinematics.hpp     # Обратная кинематика
│   │   │   ├── odometry/
│   │   │   │   └── odometry.hpp               # Однометрия
│   │   │   ├── states/
│   │   │   │   └── state_command.hpp          # State/Command
│   │   │   └── controllers/
│   │   │       ├── gait_controller.hpp        # Базовый класс походки
│   │   │       ├── pid_controller.hpp         # PID-регулятор
│   │   │       ├── trot_gait.hpp              # Рысь
│   │   │       ├── crawl_gait.hpp             # Ползание
│   │   │       ├── rest_controller.hpp        # Покой
│   │   │       └── stand_controller.hpp       # Стойка
│   │   ├── src/
│   │   │   ├── utils/, kinematics/, odometry/, states/, controllers/
│   │   │   └── nodes/
│   │   │       ├── robot_controller_node.cpp  # Главный контроллер (60 Hz)
│   │   │       ├── odometry_node.cpp          # Узел одометрии (50 Hz)
│   │   │       └── cmd_vel_pub.cpp            # Twist → RobotVelocity адаптер
│   │   ├── test/                   # 12 Google Mock файлов
│   │   ├── benchmark/              # Google Benchmark
│   │   ├── CMakeLists.txt
│   │   └── package.xml
│   │
│   ├── quadropted_msgs/            # Кастомные сообщения/сервисы
│   │   ├── msg/
│   │   │   ├── RobotModeCommand.msg    # Режим: REST/TROT/CRAWL/STAND
│   │   │   ├── RobotVelocity.msg       # robot_id + geometry_msgs/Twist
│   │   │   ├── RobotFootContact.msg    # bool[4] контакты стоп
│   │   │   └── RobotGaitCommand.msg    # Тип походки + высоты
│   │   └── srv/
│   │       └── RobotBehaviorCommand.srv # Сервис sit/up/walk
│   │
│   ├── tests/                      # Тесты корректности и бенчмарки
│   │   ├── benchmark_performance.py
│   │   ├── benchmark_cpp_vs_python.py
│   │   └── test_python_vs_cpp.py
│   │
│   ├── go1_description/            # Unitree Go1 URDF/Xacro
│   └── go2_description/            # Unitree Go2 URDF/Xacro/DAE
│
├── Makefile                      # Управление проектом (все команды)
├── README.md                       # Основная документация
├── docs/                           # Документация проекта
│   ├── benchmark-python-cpp.md
│   ├── python_vs-cpp-cross-validation.md
│   └── ...
└── scripts/                        # Утилиты
    ├── clean-current-branch.sh
    └── clean-commits.sh
```

## Описание ключевых директорий

### `src/docker/` — Инфраструктура контейнеризации
10-этапная Docker сборка с кэшированием слоёв (APT, pip, ccache). Первая сборка 15-30 мин, повторная 30-60 сек.

### `src/gazebo_sim/` — Оркестрация симуляции
Launch-файлы запускают Gazebo мир, спавнят роботов, запускают Nav2 стек для каждого робота в своём namespace.

### `src/quadropted_controller/` — Контроллеры робота
Весь контрольный стек на Python: конечный автомат режимов, контроллеры походки, кинематика, одометрия.

### `src/quadropted_msgs/` — ROS интерфейсы
4 кастомных сообщения и 1 сервис для управления роботом.

### `src/go1_description/` и `src/go2_description/` — Модели роботов
URDF/Xacro описания Unitree Go1 и Go2 с физическими свойствами и визуальными мешами.
