# Глава 2. Математические модели и методы

## 2.3 Математическая модель сегментации ground/non-ground

Задача ground segmentation заключается в разделении облака точек на два класса: точки, принадлежащие поверхности земли (ground), и точки, принадлежащие препятствиям и объектам (non-ground). От качества решения этой задачи напрямую зависит точность последующей цифровой модели высот.

### 2.3.1 Постановка задачи

Пусть $\mathcal{P} = \{\mathbf{p}_i = (x_i, y_i, z_i) \mid i = 1, \dots, N\}$ — облако точек, полученное от LiDAR. Требуется найти разбиение $\mathcal{P} = \mathcal{G} \cup \mathcal{O}$, где $\mathcal{G}$ — ground-точки, $\mathcal{O}$ — non-ground (obstacle) точки.

Основные сложности задачи: рельеф может быть неровным (холмы, овраги), что делает простой пороговый фильтр по $z$-координате неприменимым; наклонные поверхности (пандусы, склоны) являются проходимыми, но не являются строго горизонтальными; измерения содержат шум, создающий ложные ground-точки.

### 2.3.2 Алгоритм Ground Plane Fitting (GPF)

Выбран алгоритм Ground Plane Fitting, основанный на итеративном применении RANSAC для аппроксимации поверхности земли плоскостью [2].

**Шаг 1 — предварительная фильтрация.** Отбрасываются точки выше $h_{\max}$ и ниже $h_{\min}$ относительно сенсора:

$$
\mathcal{P}_{\text{filtered}} = \{\mathbf{p} \in \mathcal{P} \mid h_{\min} \leq z \leq h_{\max}\}
$$

где $h_{\max} = 0{,}5$ м, $h_{\min} = -0{,}5$ м.

**Шаг 2 — воксельное понижение.** Для повышения производительности выполняется downsampling с размером вокселя $v = 0{,}05$ м. Каждый воксель заменяется центром масс содержащихся в нём точек.

**Шаг 3 — выбор кандидатов.** Из отфильтрованного облака выбираются точки с наименьшей $z$-координатой (нижние 20%). Это увеличивает вероятность того, что выбранные точки принадлежат ground.

**Шаг 4 — итеративная подгонка плоскости.** Для каждой итерации $k = 1, \dots, K$ ($K = 3$):

1. Случайно выбираются 3 точки $\mathbf{q}_1, \mathbf{q}_2, \mathbf{q}_3$ из нижних 20%.
2. Через них проводится плоскость:

$$
\mathbf{n} \cdot \mathbf{x} + d = 0
$$

где $\mathbf{n}$ — нормаль плоскости, $d$ — смещение. Нормаль вычисляется как:

$$
\mathbf{n} = (\mathbf{q}_2 - \mathbf{q}_1) \times (\mathbf{q}_3 - \mathbf{q}_1)
$$

3. Для каждой точки $\mathbf{p} \in \mathcal{P}_{\text{filtered}}$ вычисляется расстояние до плоскости:

$$
\mathrm{dist}(\mathbf{p}) = \frac{|\mathbf{n} \cdot \mathbf{p} + d|}{\|\mathbf{n}\|}
$$

4. Точки с $\mathrm{dist}(\mathbf{p}) < \delta_k$ считаются inliers (ground). Порог $\delta_k$ увеличивается с каждой итерацией:

$$
\delta_1 = 0{,}15\ \mathrm{м},\quad \delta_2 = 0{,}25\ \mathrm{м},\quad \delta_3 = 0{,}35\ \mathrm{м}
$$

Увеличение порога позволяет захватывать неровный рельеф: первая итерация находит «ядро» плоскости, последующие присоединяют точки с отклонениями.

**Шаг 5 — пост-фильтр.** Так как порог $\delta_1 = 0{,}15$ м на первой итерации может превышать высоту LiDAR над землёй (0,095 м), точки корпуса робота на уровне $z \approx 0$ могут быть ошибочно классифицированы как ground. Пост-фильтр перемещает точки, высота которых превышает среднюю высоту ground более чем на $h_{\text{margin}} = 0{,}05$ м, в obstacle-облако.

### 2.3.3 Обоснование выбора параметров

Количество итераций $K = 3$ выбрано как компромисс между качеством сегментации и производительностью. Увеличение $K$ до 4–5 может улучшить recall на крутых склонах (>20°), но увеличивает время обработки. Пороги $\delta_k$ установлены эмпирически на основе анализа перепадов высот, характерных для пересечённой местности.
