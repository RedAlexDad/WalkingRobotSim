# Глава 2. Математические модели и методы

## 2.4 Математическая модель фильтрации облака точек

Данные LiDAR содержат шумы и некорректные измерения, которые необходимо отфильтровать перед построением карты высот. В данном разделе рассматриваются математические модели фильтров, применяемых в модуле elevation_mapping_cupy.

### 2.4.1 Фильтрация по расстоянию

Пространственная фильтрация отбрасывает точки на основании их расстояния от сенсора:

- минимальное расстояние: $d < d_{\min} = 0{,}3$ м — исключает отображение корпуса робота на карте высот;
- максимальная дальность: $d > d_{\max} = 10{,}0$ м — ограничивает рабочую зону размером карты 20×20 м и обеспечивает корректную работу visibility cleanup.

### 2.4.2 RAM-фильтр (Ramped Adaptive Maximum height)

RAM-фильтр реализует адаптивный порог высоты, зависящий от расстояния до точки. Пороговая высота $h_{\text{max}}(d)$ определяется как:

$$
h_{\text{max}}(d) =
\begin{cases}
c, & d < b \\
a \cdot (d - b) + c, & d \geq b
\end{cases}
$$

где $a = 0{,}3$ — коэффициент наклона порога, $b = 1{,}0$ м — расстояние излома, $c = 0{,}2$ м — минимальный порог высоты. На расстоянии $d = 0{,}3$ м порог составляет 0,2 м; на $d = 2{,}0$ м — 0,5 м; на $d = 10{,}0$ м — 2,9 м.

Данный фильтр позволяет на близких расстояниях отбрасывать потолки и нависающие объекты, а на дальних — учитывать естественный рельеф (например, возвышения).

### 2.4.3 Критерий Махаланобиса

Для дополнительной фильтрации выбросов используется расстояние Махаланобиса между измеренной высотой $z_{\text{measured}}$ и ожидаемой высотой $z_{\text{expected}}$ из карты:

$$
d_M = \frac{|z_{\text{measured}} - z_{\text{expected}}|}{\sigma}
$$

где $\sigma$ — стандартное отклонение шума сенсора ($\sigma = 0{,}01$ м). Если $d_M > \delta_M$ ($\delta_M = 2{,}0$), точка отбрасывается. Критерий $d_M > 2$ соответствует отклонению более чем на $2\sigma$.

### 2.4.4 Visibility Cleanup

Visibility cleanup — алгоритм, корректирующий карту высот на основе анализа видимости. Для каждой ячейки карты определяется, находится ли она на прямой видимости между сенсором и точкой измерения. Если ячейка находится на линии видимости, её уверенность $c_{ij}$ уменьшается:

$$
c_{ij} \gets c_{ij} - \Delta c, \quad \Delta c = 0{,}1
$$

где $\Delta c$ — шаг очистки (cleanup_step). Алгоритм обеспечивает: автоматическое удаление динамических объектов через несколько кадров; предотвращение создания ложных объектов из шумовых измерений. Радиус очистки (cleanup_footprint) определяет количество соседних ячеек, обрабатываемых за одну итерацию.

### 2.4.5 Комплексный pipeline фильтрации

Каждая точка последовательно проходит этапы:

1. проверка $d \geq d_{\min}$ — иначе отброс;
2. проверка $d \leq d_{\max}$ — иначе отброс;
3. проверка $z \leq h_{\text{max}}(d)$ — иначе отброс;
4. проецирование в фрейм карты через TF-трансформацию;
5. проверка $d_M \leq \delta_M$ — иначе отброс;
6. обновление карты: взвешенное усреднение высоты с учётом дисперсии.
