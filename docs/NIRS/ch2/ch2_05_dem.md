# Глава 2. Математические модели и методы

## 2.5 Математическая модель цифровой модели высот

Цифровая модель высот (Digital Elevation Model, DEM) является центральным элементом terrain-aware планирования, предоставляя информацию о рельефе для всех последующих вычислений проходимости и планирования пути.

### 2.5.1 Структура карты высот

Карта высот представляет собой регулярную сетку размером $W \times H$ ячеек с разрешением $r$. Каждая ячейка $(i, j)$ содержит:

- средневзвешенную высоту $z_{ij}$;
- дисперсию оценки $\sigma_{ij}^2$;
- количество наблюдений $n_{ij}$;
- временную метку последнего обновления $t_{ij}$.

Параметры карты: $W = H = 200$ ячеек, $r = 0{,}1$ м, размер карты $20 \times 20$ м.

### 2.5.2 Проецирование точек в ячейки карты

Для каждой точки облака $\mathbf{p} = (x, y, z)$ в системе координат карты определяется индекс ячейки:

$$
\begin{aligned}
\mathrm{cell}_x &= \left\lfloor \frac{x - \mathrm{mapOrigin}_x}{r} \right\rfloor \\
\mathrm{cell}_y &= \left\lfloor \frac{y - \mathrm{mapOrigin}_y}{r} \right\rfloor
\end{aligned}
$$

### 2.5.3 Взвешенное усреднение высоты

При поступлении нового измерения $z_k$ в ячейку $(i, j)$ обновлённая высота вычисляется как взвешенное среднее:

$$
z_{ij}^{(k+1)} = \frac{w_{ij}^{(k)} \cdot z_{ij}^{(k)} + w_k \cdot z_k}{w_{ij}^{(k)} + w_k}
$$

где вес измерения обратно пропорционален дисперсии шума сенсора:

$$
w_k = \frac{1}{\sigma_{\text{sensor}}^2}, \quad \sigma_{\text{sensor}} = 0{,}01\ \mathrm{м}
$$

### 2.5.4 Обновление дисперсии

Дисперсия ячейки обновляется по правилу фильтра Калмана. При каждом новом измерении дисперсия уменьшается, отражая повышение уверенности:

$$
\sigma_{ij}^{2(k+1)} = \frac{1}{\frac{1}{\sigma_{ij}^{2(k)}} + \frac{1}{\sigma_{\text{sensor}}^2}}
$$

Начальная дисперсия устанавливается равной $\sigma_0^2$ (априорная неопределённость). При отсутствии измерений дисперсия остаётся высокой, указывая на недостоверность данных в данной ячейке.

### 2.5.5 Gradient поверхности

Gradient (уклон) поверхности вычисляется в окне $3 \times 3$ методом PCA. Для каждой ячейки $(i, j)$ формируется матрица координат соседних точек:

$$
\mathbf{X} = \begin{bmatrix}
x_{i-1,j-1} & y_{i-1,j-1} & z_{i-1,j-1} \\
\vdots & \vdots & \vdots \\
x_{i+1,j+1} & y_{i+1,j+1} & z_{i+1,j+1}
\end{bmatrix}_{9 \times 3}
$$

Вычисляется ковариационная матрица $\mathbf{C} = \mathbf{X}^T \mathbf{X}$. Собственный вектор $\mathbf{n} = (n_x, n_y, n_z)$, соответствующий минимальному собственному значению, является нормалью к поверхности. Угол наклона:

$$
\mathrm{slope} = \arccos(|n_z|), \qquad \mathrm{aspect} = \mathrm{atan2}(n_y, n_x)
$$

Значение gradient нормализовано: $0{,}0$ — горизонтальная поверхность, $1{,}0$ — вертикальная стена.

### 2.5.6 Roughness поверхности

Шероховатость (roughness) вычисляется как среднеквадратичное отклонение высот в окне $N \times N$ ($N = 5$, что составляет $0{,}5 \times 0{,}5$ м):

$$
\mathrm{roughness}(i,j) = \sqrt{\frac{1}{N^2} \sum_{\substack{k = i-m \\ l = j-m}}^{i+m,\,j+m} \bigl(z(k,l) - z_{\mathrm{avg}}\bigr)^2}, \quad m = \left\lfloor\frac{N}{2}\right\rfloor
$$

Интерпретация roughness: менее $0{,}01$ м — ровная дорога; $0{,}01{-}0{,}03$ м — гладкий асфальт; $0{,}03{-}0{,}06$ м — трава, гравий; $0{,}06{-}0{,}10$ м — мелкие камни; более $0{,}10$ м — крупные камни.

### 2.5.7 Фильтры постобработки

После обновления карты применяются следующие фильтры.

**Медианный фильтр** (окно $3 \times 3$): значение в ячейке заменяется на медиану в окне. Удаляет импульсный шум, сохраняя границы объектов.

**Фильтр по уверенности**: ячейки с высокой дисперсией ($\sigma_{ij}^2 > \sigma_{\text{max}}^2$) заполняются интерполяцией от соседних ячеек для закрытия мелких пропусков в данных.

**Фильтр выбросов**: если высота ячейки отличается от среднего по окну $3 \times 3$ более чем на $3\sigma_{ij}$, ячейка помечается как недостоверная.
