[pytest]
testpaths = tests
markers =
    gpu: requires GPU / CuPy (skip if no GPU)
    slow: slow tests that may need longer timeout
    semantic: tests for semantic fusion kernels
    plugin: tests for plugin pipeline modules
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
