# Отчёт: отладка TrotGait — устойчивая ходьба Go2 в Isaac Sim

- **Дата:** 13 сентября 2026
- **Ветка:** `feat/isaam-research`
- **Статус:** достигнута устойчивая ходьба (без падений), остаточная дуга
- **Итог:** робот проходит ~4.8 м вперёд, Z стабильна (std 0.017 м),
  дрейф по Y снижен с 2.82 до 1.56 м

---

## 1. Задача

Довести математический IK/TROT-контроллер (Rust, ROS2) до устойчивой
ходьбы Unitree Go2 в Isaac Sim 6.0.1 / IsaacLab 3.0. Исходно робот падал
сразу после перехода в режим TROT.

## 2. Оборудование и ПО

| Компонент | Значение |
|---|---|
| Симулятор | Isaac Sim 6.0.1 |
| Среда | IsaacLab 3.0 (ManagerBasedEnv) |
| Робот | Unitree Go2, 12 суставов (ассет IsaacLab `go2.usd`) |
| Физика | PhysX, GPU; dt = 0.005 с |
| ОС / GPU | Ubuntu 26.04, NVIDIA RTX 5070 Ti, драйвер 595.91.07 |
| Контроллер | Rust (`quadropted_controller_rust`), 60 Гц, в контейнере |
| Мост | `/robot1/joint_group_controller/commands` (Float64MultiArray) |
| Телеметрия | `src/isaac/run_sim_telemetry.py` → CSV |

## 3. Диагностика (по усиленному логу)

Первым шагом включено максимальное логирование
(`src/isaac/run_sim_telemetry.py`): структурированный `[SIM] REPORT`
(поза, углы, `joint_err`, fps, статус IMU), детекция NaN/Inf, лог смены
команды, публикация IMU в `/robot1/imu`. Исправлено время шага
(`timeline.get_current_time()` застревало → `step·physics_dt`).

По логам и численной модели походки (порт в Python) выявлены три
корневые причины падения.

### Проблема 1. Дрейф стопы в stance → насыщение IK

**Симптом:** робот падает за ~1–2 с в TROT; `joint_err` до 1.4 рад;
команда `calf = 0` (нога полностью выпрямлена).

**Причина:** в `TrotStanceController::position_delta` скорость стопы
считалась как `-(step_dist/4)/(dt·stance_ticks)`. Делитель
`stance_ticks` — длина ОДНОЙ stance-фазы (9 тиков), но в трот-расписании
нога стоит на земле 3 фазы подряд (27 тиков). Стопа уезжала назад втрое
дальше, чем возвращал swing (`foot_x` до −1.2 м), IK уходил в насыщение.

**Решение:** физически корректная скорость стопы в системе тела —
`velocity = -cmd_vel` (стопа фиксирована в мире) + прежняя
z-компенсация. Файл: `quadropted-core/src/controllers/trot/stance.rs`.

### Проблема 2. Накопление IMU-компенсации

**Симптом:** крен/тангаж накапливались, hip рос до clamp ±0.3 → закрутка.

**Причина:** компенсация (поворот стоп по крену/тангажу) применялась к
`self.foot_locations` каждый тик — повёрнутые стопы становились входом
stance следующего тика.

**Решение:** компенсация применяется только к копии стоп для IK, не к
состоянию походки.

### Проблема 3. Инвертированный знак IMU-компенсации

**Симптом:** робот переворачивался (крен до 180°).

**Причина:** `comp = kp·(0−θ) = −kp·θ`, а применялся `R(−comp) =
R(+kp·θ)` — компенсация усиливала наклон вместо устранения.

**Решение:** применяется `R(comp) = R(−kp·θ)`, `kp` поднят 0.15 → 1.0.

### Проблема 4. Постоянный момент крена/курса

**Симптом:** робот уходил по дуге (дрейф по Y, медленный поворот yaw).

**Решение:** (а) симметричная стойка (убрано смещение переда вперёд
`dx_front = body_length/2 + 0.02`); (б) yaw-стабилизация —
P-регулятор курса `gait_cmd[2] += -0.5·(imu_yaw − desired_yaw)`.

## 4. Результаты

Все прогоны: vx = 0.3 м/с, плоская поверхность, ~17 000–22 000 шагов.

| Прогон | distance_x, м | z_mean, м | z_std, м | max_roll,° | drift_y, м | CoT |
|---|---|---|---|---|---|---|
| База (без IMU) | — (дуга) | 0.232 | — | переворот | 2.5+ | — |
| signfix (kp=1.0) | 5.66 | 0.244 | 0.016 | 31.9 | 3.12 | 2.58 |
| + yaw-стаб. | 4.84 | 0.246 | 0.016 | 35.1 | 2.09 | 2.48 |
| + симметричная стойка | 4.77 | 0.246 | 0.017 | 33.4 | **1.56** | 2.50 |

**Итоговый результат (последний прогон):**
- пройдено вперёд **4.77 м** (путь 6.46 м), падений нет;
- высота стабильна: `z_mean = 0.246 м`, `z_std = 0.017 м`;
- `joint_err` 0.05–0.24 рад; NaN нет;
- дрейф по Y **1.56 м** (снижен с 2.82 м).

## 5. Остаточное ограничение

Крен корпуса ~24° (max 33°) сохраняется. Простой P-стабилизатор
поворачивает стопы горизонтально, но не создаёт возвращающего момента
для выпрямления корпуса; именно этот крен даёт остаточную дугу.
Полное выпрямление требует **capture-point балансира** (смещение стоп
по боковой скорости корпуса). Повышение `kp` до 2.0 дестабилизирует
(робот падает) — проверено.

## 6. Артефакты

- Метрики (JSON): `reports/isaam/telemetry/summary-trot-debug-2026-09-13.json`.
- Компактная телеметрия победного прогона (прорежена ×10, gzip):
  `reports/isaam/telemetry/telemetry_ik_sym_2026-09-13_decimated.csv.gz`
  (1691 строка; полный CSV — 16 897 строк).
- Сырые CSV телеметрии: `logs/isaac/telemetry_ik_*.csv` (каталог `logs/`
  в `.gitignore`, в репозиторий не попадает).
- Инструменты: `src/isaac/telemetry.py`,
  `src/isaac/run_sim_telemetry.py`, `src/isaac/analyze_telemetry.py`,
  `src/isaac/run_isaaclab.sh`.
- Обоснование/журнал: `docs/ieee-article/data-collection.md`.

## 7. Воспроизведение

```bash
# 1. Контроллер в контейнере
docker exec -d walking_robot_sim bash -lc "source /opt/ros/jazzy/setup.bash && \
  source /root/ws/install/setup.bash && \
  ros2 run quadropted_controller_rust robot_controller_node --ros-args -r __ns:=/robot1"

# 2. IsaacLab с телеметрией
GO2_TELEMETRY_TAG=ik bash src/isaac/run_isaaclab.sh --headless

# 3. Команда вперёд
ros2 topic pub -1 /robot1/robot_mode quadropted_msgs/msg/RobotModeCommand \
  '{mode: "TROT", robot_id: 1}'
ros2 topic pub -1 /robot1/robot_velocity quadropted_msgs/msg/RobotVelocity \
  '{robot_id: 1, cmd_vel: {linear: {x: 0.3}}}'

# 4. Разбор
python3 src/isaac/analyze_telemetry.py logs/isaac/telemetry_ik_*.csv --plot
```

## 8. Коммиты

`5d9fb74` (дрейф стопы), `fc31e91` (IMU-компенсация),
`9e74fa6` (yaw-стабилизация), `3d45645` (симметричная стойка),
`0eddbf4` (настраиваемый предел hip).

## 9. RL-базлайн (в работе)

Для сравнения (статья заявлена как RL vs IK) в `go2_policy.py`
(автономный запуск политики NVIDIA) добавлена запись телеметрии в тот
же CSV-формат: поза, кватернион, скорости, углы/скорости суставов;
`cmd0..2` — команда скорости `(vx,vy,wz)`. Данные берутся из `go2.robot`
(`get_world_poses`, `get_velocities`, `get_dof_positions/velocities`)
через `wp.to_torch`. Коммит `cdca72b`.

**Статус:** первый прогон дошёл до политики (телеметрия писала CSV), но
падал на индексации `wp.array` — исправлено. Повторные запуски
`go2_policy.py` зависают на старте Isaac («Simulation App Starting»);
GPU свободен, зомби-процессов нет — вероятно, залипшее состояние Kit
после первого прерывания по `timeout`. Требуется отладка запуска
(сброс кэша Kit / запуск через `setsid` без `timeout`).

**Решение:** причина зависания — испорченный кэш Kit
(`DerivedDataCache` / `nv_shadercache`) после прерывания. После переноса
кэша старт снова быстрый (~10 с). Добавлены `--duration N` (остановка
через N секунд симуляции) и тайминги фаз `[phase +Xs]`.

**Результат RL-базлайна (vx=0.3, 15 с):**

| Метрика | RL (NVIDIA) | IK/TROT |
|---|---|---|
| Пройдено вперёд | 3.34 м | ~4.8 м (за 85 с) |
| Средняя скорость | 0.222 м/с | ~0.20 м/с |
| Z (mean / std) | 0.159 / 0.041 м | 0.246 / 0.017 м |
| **max roll** | **2.3°** | 24° |
| **дрейф по Y** | **0.37 м** | 1.56 м |
| Время до устойчивой ходьбы | 0.22 с | — |

**Вывод:** обученная политика устойчивее (почти без крена и дрейфа),
модельный контроллер держит высоту стабильнее, но имеет остаточный
крен и дугу. CoT для RL требует пересчёта с его PD-параметрами
(kp=25), в таблице приведён с общими (завышен).

## 10. Что осталось до статьи

1. Запустить RL-базлайн (vx=0.3, 30–40 с), снять CSV.
2. Повторы ≥3 для IK и RL → `mean±std` (скорость, Z, дрейф, CoT).
3. Обновить числа в черновике статьи (уйти от «18 400 шагов» к новым:
   IK — 4.77 м, Z 0.246±0.017, дрейф 1.56 м).
4. Соавторы/аффилиация, рисунки (X(t),Y(t),Z(t), углы, схема).

## 11. Статистика (повторы ≥3, vx=0.3)

Источник: `reports/isaam/telemetry/stats-rl-vs-ik-2026-09-13.json`.

| Метрика | RL (NVIDIA), n=3 | IK/TROT, n=3 |
|---|---|---|
| Длительность прогона | 15.0 с | 19.3 ± 2.1 с |
| Пройдено вперёд | 3.34 ± 0.00 м | 2.75 ± 0.12 м |
| Средняя скорость | 0.231 ± 0.000 м/с | 0.215 ± 0.012 м/с |
| Z (mean) | 0.159 ± 0.000 м | 0.235 ± 0.001 м |
| Z (std) | 0.040 ± 0.000 м | 0.025 ± 0.001 м |
| **max roll** | **2.3 ± 0.0°** | 27.8 ± 3.7° |
| max pitch | 4.1 ± 0.0° | 35.6 ± 0.0°* |
| **дрейф по Y** | **0.37 ± 0.00 м** | 1.09 ± 0.16 м |

\* max pitch IK — стартовый выброс при падении робота со спавн-высоты;
установившийся тангаж ~2°.

**Ключевые выводы для статьи:**
- Скорости сопоставимы (~0.22 м/с у обоих).
- RL существенно устойчивее: крен 2.3° против 27.8°, дрейф 0.37 против
  1.09 м — обученная политика держит курс и не валится.
- IK стабильнее держит высоту (`z_std` 0.025 против 0.040) и полностью
  **детерминирован** (std по всем метрикам RL = 0 — симуляция
  детерминирована, повторы идентичны).
- Остаточный крен IK (проблема 28) — главное отличие; требуется
  capture-point/дифференциальная длина ног.

## 12. Серия скоростей (рабочая область)

Источник: `reports/isaam/telemetry/speed-series-2026-09-13.json`
(по одному прогону на скорость, 15 с).

| vx (команда) | RL: скорость / roll / дрейф | IK: скорость / roll / дрейф |
|---|---|---|
| 0.1 м/с | 0.026 / 2.6° / 0.09 м | **0.001 / 69° / 0.28 м** ✗ |
| 0.2 м/с | 0.116 / 3.6° / 0.21 м | **0.024 / 47° / 0.23 м** ✗ |
| 0.3 м/с | 0.222 / 2.3° / 0.37 м | 0.141 / 25° / 0.77 м |
| 0.4 м/с | 0.344 / 3.0° / 0.80 м | **0.066 / 31° / 0.60 м** ✗ |

**Ключевой результат:** RL-политика отслеживает скорость во всём
диапазоне 0.1–0.4 м/с (roll 2–4°). Модельный IK/TROT-контроллер
**устойчив только около 0.3 м/с** — на 0.1, 0.2 и 0.4 м/с крен
47–69° и падение. Это даёт чёткую **рабочую область** каждого подхода
и усиливает сравнение (не только «кто лучше», но «где применим»).

## 13. Внешние возмущения (механизм готов)

Реализован механизм бокового толчка в `run_sim_telemetry.py`:
ROS-подписчик `PushSubNode` на топик **`/robot1/push`**
(`std_msgs/Float64`, сила в Н, ось Y). При получении сообщения сила
прикладывается к базе через `Articulation.set_external_force_and_torque`
в течение `dur` секунд (`GO2_PUSH_DUR`, по умолчанию 0.2 с → 40 шагов).
Толчок можно подать **в любой момент** (после старта TROT):

```bash
ros2 topic pub -1 /robot1/push std_msgs/msg/Float64 '{data: 60.0}'
```

Проверено: `[SIM] PUSH force=60.0N (y), 40 шагов`. Теперь сценарии
возмущений воспроизводимы. **Осталось:** серия экспериментов — порог
силы и сравнение восстановления RL vs IK (толчок сбоку, скользкая
поверхность).

## 14. Формулировка «научного крючка» (Limitations + Conclusion)

В `draft-article.md` (§6.2, §7) внесено:
- **Limitations:** разные ассеты/PD, узкая рабочая область IK,
  только симуляция.
- **Conclusion:** остаточный крен IK — **не дефект реализации, а
  воспроизводимый предел простого P-стабилизатора ориентации**:
  увеличение kp (1.0→2.0), расширение предела hip (0.3→0.6) и инверсия
  знака компенсации **ухудшают** устойчивость; лишь симметричная стойка
  и стабилизация курса снижают дрейф. Указывает путь к улучшению
  (capture-point / дифференциальная длина ног).

## 15. План улучшений и статус

Полный план с приоритетами и статусами выполнения —
`docs/ieee-article/improvement-plan.md` (§0 — таблица статусов).

## 16. Схема телеметрии (125 колонок)

`src/isaac/telemetry.py` пишет максимально полный CSV (единый для IK и
RL). Колонки:

| Группа | Колонки |
|---|---|
| Время | `step`, `sim_time`, `wall_utc` |
| Позиция/ориентация | `x,y,z`, `qw,qx,qy,qz`, `roll,pitch,yaw` |
| Скорости | `vx,vy,vz` (мир), `vx_b,vy_b,vz_b` (тело), `wx,wy,wz` |
| Ускорения/гравитация | `ax,ay,az` (IMU, тело), `gx,gy,gz` (гравитация в теле) |
| Команда/режим | `vel_cmd_x/y/z`, `mode` |
| Флаги | `fallen`, `hip_sat`, `nan_flag` |
| Стопы | `foot_x/y/z0..3` (мир), `contact0..3` |
| Суставы | `q0..11`, `dq0..11` |
| Динамика | `tau0..11` (оценка момента), `p0..11` (мощность), `power_w`, `energy_j` |
| Ошибки/команды | `err0..11` (`cmd−q`), `cmd0..11` |

Стопы берутся из articulation по телам `*_foot` (`foot_ids=[15..18]`).
`tau_i = kp·(cmd_i−q_i) − kd·dq_i`; `energy_j` — накопленная энергия.
`kp/kd` задаются через `GO2_KP/GO2_KD` (IK: 75/0.5, RL: 25/0.5).

## 17. Влияние логирования на fps и две проблемы

**Проблема A: тяжёлый лог замедлял симуляцию.** Расширение CSV до 125
колонок уронило fps с ~195 до ~124. Rust-контроллер работает по
**wall-clock 60 Гц**, а симуляция — по шагам; при замедлении симуляции
частота обновления команд относительно шагов физики меняется → другое
поведение IK (крен вырос, разброс). RL не затронут (пошаговый,
детерминированный). **Это само по себе результат для статьи:** модельный
контроллер зависит от реального времени, обученная политика — нет.

**Решение A:** батч-перевод GPU→CPU (`_snapshot`: один `.cpu()` за шаг
вместо десятков синхронизаций) + `flush_every 200` + запись CSV в
фоновом потоке → fps 124→159. **Корневое решение (A1):** контроллер
переведён на **сим-время** — лаунчер публикует `/robot1/sim_time`
(`Float64MultiArray`), Rust-контроллер шагает при приросте сим-времени
на 1/60 с (fallback — wall-clock). Проверено: ровно 60 тиков/сим-с
(120 за 2 с) независимо от fps → результаты IK больше не зависят от
загрузки CPU.

**Проблема B: нестабильный старт `go2_policy.py`.** Повторные запуски
зависают на старте Isaac (залипает кэш Kit). **Решение B:** сброс
lock-файлов Kit и retry-петля в скрипте повторов RL.

**Баги, найденные и исправленные при этом:**
- `timeline.get_current_time()` застревал → время = `step·dt`;
- RL-путь: `cmd` (список, CPU) смешивался с CUDA-тензорами в `torch.cat`
  → лог не писался; исправлено приведением к device obs (`bcf1bd9`).

Коммиты: `1712d4b` (оптимизация), `bcf1bd9` (device-фикс).
