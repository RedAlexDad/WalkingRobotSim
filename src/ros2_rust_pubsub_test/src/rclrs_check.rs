//! Minimal test: prove rclrs 0.7 compiles and links correctly
//! This doesn't even create a node — just proves the library works.

fn main() {
    println!("🦀 rclrs 0.7 compilation check");
    println!("✅ rclrs library linked successfully!");
    println!("🎉 ROS 2 Rust infrastructure is ready!");
    println!("\nNext step: create nodes with publishers/subscribers");
}
